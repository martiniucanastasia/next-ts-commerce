export type Product = {
  id: number;
  name: string;
  category: string;
  rating: number;
  price: number;
  seller: string;
  brand: string;
  condition: string;
  verified: boolean;
  image: string;
};

export type currentOffer = {
  id: number;
  value: string;
};

